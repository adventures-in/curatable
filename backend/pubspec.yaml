name: backend
description: a backend for news_curation app
# version: 0.1.0
# homepage: https://www.example.com
publish_to: none

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  shelf:
  googleapis:
  googleapis_auth:
  http:
  collection:

dev_dependencies:
  lints:
  test:
